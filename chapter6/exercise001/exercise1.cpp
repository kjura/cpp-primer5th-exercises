#include <iostream>
#include <vector>
#include <cmath>
#include <string>

// g++ -std=c++20 -pedantic -Wall -Wextra -Werror -Wshadow -Wsign-conversion -g sandbox.cpp  -o sandbox
// g++ -std=c++20 -pedantic -Wall -Wextra -Wsign-conversion -g sandbox.cpp -o sandbox
// <br />
// PLACEHOLDEREXERCISENAME < input.txt > output.txt
// PLACEHOLDEREXERCISENAME ./exercise5 < input.txt >> output.txt
using std::cout;
using std::endl;
using std::vector;
using std::string;
using std::cin;




int main()
{

    /* 
    
    Going by the book:


    Parameters == Local variables declared inside the function parameter list.
    Parameters are initialized by the arguments provided in each function call.


    Arguments == Values supplied in a function call that are used to initialize the
    function’s parameters.
    
    */

    /* 
   
    In practice, arguments are what we put into a function call and parameters are integral part of a function's declaration
   
    */


    return 0;
}
